# Copy this file to .env and fill in secrets. DO NOT commit real secrets.
# Example values are placeholders — replace before use.

# Postgres
POSTGRES_USER=liveshop
POSTGRES_PASSWORD=replace_me_with_strong_password
POSTGRES_DB=liveshop

# Redis (optional)
REDIS_PASSWORD=replace_me_with_strong_password

# MinIO (S3-compatible object storage)
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=replace_me_with_strong_password

# API/Services
API_PORT=3001
STREAMING_PORT=3002
